---
# Mirrors the authentik_app pattern: Docker from the official repo, then a compose
# stack. (A shared 'docker' role would DRY the install across both — noted, not
# done here.)
- name: Add Docker's official APT repository
  become: true
  ansible.builtin.deb822_repository:
    name: docker
    types: [deb]
    uris: "https://download.docker.com/linux/debian"
    suites: "{{ ansible_distribution_release }}"
    components: [stable]
    signed_by: https://download.docker.com/linux/debian/gpg

- name: Install Docker Engine + Compose v2 plugin
  become: true
  ansible.builtin.apt:
    name:
      - docker-ce
      - docker-ce-cli
      - containerd.io
      - docker-compose-plugin
    state: present
    update_cache: true

- name: Create the monitoring directories
  become: true
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    owner: root
    group: root
    mode: "0755"
  loop:
    - "{{ monitoring_compose_dir }}"
    - "{{ monitoring_config_dir }}"
    - "{{ monitoring_config_dir }}/grafana/provisioning/datasources"
    - "{{ monitoring_config_dir }}/grafana/provisioning/dashboards"
    - "{{ monitoring_config_dir }}/grafana/dashboards"

- name: Render the monitoring config files
  become: true
  ansible.builtin.template:
    src: "{{ item.src }}"
    dest: "{{ monitoring_config_dir }}/{{ item.dest }}"
    owner: root
    group: root
    mode: "0644"
  loop:
    - { src: scrape.yml.j2, dest: scrape.yml }
    - { src: alert-rules.yml.j2, dest: alert-rules.yml }
    - { src: alertmanager.yml.j2, dest: alertmanager.yml }
    - { src: ntfy.yml.j2, dest: ntfy.yml }
    - { src: blackbox.yml.j2, dest: blackbox.yml }
    - { src: snmp.yml.j2, dest: snmp.yml }
    - { src: grafana-datasource.yml.j2, dest: grafana/provisioning/datasources/datasource.yml }
    - { src: grafana-dashboards.yml.j2, dest: grafana/provisioning/dashboards/dashboards.yml }
    - { src: dashboards/lab-overview.json.j2, dest: grafana/dashboards/lab-overview.json }
    - { src: dashboards/lab-services-nas.json.j2, dest: grafana/dashboards/lab-services-nas.json }
  notify: Restart monitoring

- name: Render the monitoring env (secrets)
  become: true
  ansible.builtin.template:
    src: env.j2
    dest: "{{ monitoring_compose_dir }}/.env"
    owner: root
    group: root
    mode: "0600"
  notify: Restart monitoring

- name: Render the monitoring compose
  become: true
  ansible.builtin.template:
    src: compose.yaml.j2
    dest: "{{ monitoring_compose_dir }}/compose.yaml"
    owner: root
    group: root
    mode: "0640"
  notify: Restart monitoring

- name: Start the monitoring stack
  become: true
  community.docker.docker_compose_v2:
    project_src: "{{ monitoring_compose_dir }}"
    state: present
    pull: missing

# ntfy auth: deny-all by default, so provision the publisher + subscriber users and
# their topic ACLs. Idempotent — the add is skipped if the user already exists.
- name: Read ntfy users (waits for ntfy, doubles as the idempotency check)
  become: true
  ansible.builtin.command:
    cmd: "docker compose -f {{ monitoring_compose_dir }}/compose.yaml exec -T ntfy ntfy user list"
  register: monitoring_ntfy_users
  retries: 10
  delay: 3
  until: monitoring_ntfy_users.rc == 0
  changed_when: false

- name: Create the ntfy users
  become: true
  ansible.builtin.command:
    argv:
      - docker
      - compose
      - -f
      - "{{ monitoring_compose_dir }}/compose.yaml"
      - exec
      - -T
      - -e
      - "NTFY_PASSWORD={{ item.password }}"
      - ntfy
      - ntfy
      - user
      - add
      - "{{ item.name }}"
  loop:
    - { name: alertmanager, password: "{{ monitoring_ntfy_alertmanager_password }}" }
    - { name: "{{ monitoring_ntfy_user }}", password: "{{ monitoring_ntfy_user_password }}" }
  loop_control:
    label: "{{ item.name }}"
  when: item.name not in monitoring_ntfy_users.stdout
  changed_when: true
  no_log: true

- name: Set the ntfy topic ACLs (least privilege)
  become: true
  ansible.builtin.command:
    cmd: "docker compose -f {{ monitoring_compose_dir }}/compose.yaml exec -T ntfy ntfy access {{ item.user }} {{ monitoring_ntfy_topic }} {{ item.perm }}"
  loop:
    - { user: alertmanager, perm: write-only }
    - { user: "{{ monitoring_ntfy_user }}", perm: read-only }
  loop_control:
    label: "{{ item.user }}"
  changed_when: false

# Kiosk playlist: rotate through every dashboard tagged `monitoring_kiosk_tag`. The
# Grafana file provisioner doesn't manage playlists, so create it via the API. Tag-
# based so new rotated dashboards (e.g. the CCTV one) join just by carrying the tag.
- name: List existing Grafana playlists (waits for Grafana, doubles as idempotency)
  ansible.builtin.uri:
    url: "http://127.0.0.1:3000/api/playlists?query={{ monitoring_kiosk_playlist_name | urlencode }}"
    method: GET
    url_username: "{{ monitoring_grafana_admin_user }}"
    url_password: "{{ monitoring_grafana_admin_password }}"
    force_basic_auth: true
    status_code: [200]
  register: monitoring_grafana_playlists
  retries: 10
  delay: 3
  until: monitoring_grafana_playlists.status == 200
  no_log: true

- name: Create the kiosk playlist
  ansible.builtin.uri:
    url: "http://127.0.0.1:3000/api/playlists"
    method: POST
    url_username: "{{ monitoring_grafana_admin_user }}"
    url_password: "{{ monitoring_grafana_admin_password }}"
    force_basic_auth: true
    body_format: json
    body:
      name: "{{ monitoring_kiosk_playlist_name }}"
      interval: "{{ monitoring_kiosk_rotate_interval }}"
      items:
        - type: dashboard_by_tag
          value: "{{ monitoring_kiosk_tag }}"
          order: 1
    status_code: [200]
  when: monitoring_grafana_playlists.json | selectattr('name', 'equalto', monitoring_kiosk_playlist_name) | list | length == 0
  no_log: true
