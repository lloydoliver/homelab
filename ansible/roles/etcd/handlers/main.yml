---
- name: Reload systemd
  become: true
  ansible.builtin.systemd_service:
    daemon_reload: true

- name: Restart etcd
  become: true
  ansible.builtin.systemd_service:
    name: etcd
    state: restarted
