---
# Named distinctly from base's "Apply netplan" so the two roles' handlers don't
# collide in a combined play. netplan apply reconfigures all files either way.
- name: Apply incus netplan
  become: true
  ansible.builtin.command:
    cmd: netplan apply
  changed_when: true
